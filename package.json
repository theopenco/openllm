{
	"name": "openllm",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"build": "turbo run build",
		"clean": "find . -path \"./node_modules\" -prune -o \\( -type d -name \"dist\" -o -name \".turbo\" -o -name \".source\" -o -name \".vercel\" -o -name \".vinxi\" -o -name \".next\" \\) -exec rm -rf {} +",
		"clean:node_modules": "find . -type d -name \"node_modules\" -exec rm -rf {} +",
		"dev": "turbo run dev",
		"format": "eslint --fix . && prettier --write .",
		"generate": "turbo run generate",
		"postinstall": "husky && sync-editorconfig || true",
		"lint": "eslint . && prettier --check .",
		"push-dev": "pnpm --filter db push",
		"push-test": "cross-env-shell \"DATABASE_URL=${DATABASE_URL:-postgres://postgres:pw@localhost:5389/test}\" pnpm --filter db push",
		"reset": "pnpm --filter db reset",
		"seed": "pnpm --filter db seed",
		"setup": "docker compose down -v && docker compose up -d && sleep 5 && pnpm push-test && pnpm push-dev && pnpm seed",
		"sync": "pnpm push-dev; pnpm push-test",
		"test": "cross-env-shell \"DATABASE_URL=${DATABASE_URL:-postgres://postgres:pw@localhost:5389/test}\" vitest run --no-file-parallelism",
		"test:e2e": "cross-env-shell \"DATABASE_URL=${DATABASE_URL:-postgres://postgres:pw@localhost:5389/test}\" vitest run -c vitest/vitest.e2e.config.mts --no-file-parallelism"
	},
	"commitlint": {
		"extends": "@steebchen/commitlint-config"
	},
	"prettier": "@steebchen/prettier-config",
	"dependencies": {
		"gpt-tokenizer": "2.9.0"
	},
	"devDependencies": {
		"@steebchen/commitlint-config": "^1.5.2",
		"@steebchen/eslint-config": "^1.5.2",
		"@steebchen/lint-next": "^1.19.4",
		"@steebchen/prettier-config": "^1.4.2",
		"concurrently": "^9.1.2",
		"cross-env-shell": "7.0.3",
		"dotenv": "16.5.0",
		"esbuild-fix-imports-plugin": "1.0.20",
		"eslint": "^9.24.0",
		"husky": "^9.1.7",
		"prettier": "^3.5.3",
		"prettier-plugin-tailwindcss": "^0.6.11",
		"tsup": "8.4.0",
		"tsx": "^4.19.3",
		"turbo": "^1.10.0",
		"typescript": "5.8.3",
		"vite": "^6.3.4",
		"vite-tsconfig-paths": "^5.1.4",
		"vitest": "^3.1.1"
	},
	"disabledLinters": [
		"eslint-plugin-tailwindcss"
	]
}
